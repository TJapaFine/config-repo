amazon:
  aws:
    dynamo-db:
      service-endpoint: http://localhost:8000
      access-key: fakekey
      secret-key: fakesecret
    
    kafka:
      server-config: localhost:29092
      group-id: terraesa